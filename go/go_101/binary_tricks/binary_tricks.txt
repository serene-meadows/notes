finding max int and max uint
  // problem
  // will only work on 64 bits
  // on 32 bits it will throw error
  // since not convertible to uint for 32bit system
  const MaxUint uint = (1 << 64) - 1

  // solution - maxUint
  const MaxUint = ^uint(0)
  // solution - maxInt
  const maxInt = int(^uint(0) >> 1)





finding native word size
finding is 32 or 64 bit system
  // solution
  const NativeWordBits = 32 << (^uint(0) >> 63)
  const Is64BitOS      = ^uint(0) >> 63 != 0
  const Is32BitOS      = ^uint(0) >> 32 == 0
